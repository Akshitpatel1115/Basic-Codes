age = input("Enter your age = ")  # when we take input from user then it always store as string.

print(age,type(age))

# if we need to store age as a int (SAME FOR ANOTHER DATA TYPE)

age = int(input("Enter your age = "))

print(age,type(age))

