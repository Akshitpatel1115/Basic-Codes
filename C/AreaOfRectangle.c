#include <stdio.h>

int main(){
    int length,breadth;
    length = 4;
    breadth = 5;
    // printf("Enter the value of length : ");
    // scanf("%d",&length);
    // printf("Enter the value of breadth : ");
    // scanf("%d",&breadth);
    printf("The area of Rectangele is : %d",length*breadth);
    return 0;
}