#include <stdio.h>

int main(){
    int a = 45;
    int b = 65;
    // condition ? expression-if-ture : expression-if-false
    a>b ? printf("a is greater") : printf("b is greater");  // this is short method to use if-else statement
    return 0;
}